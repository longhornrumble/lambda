{
  "name": "notification-hub-lambda",
  "version": "1.0.0",
  "description": "Reusable notification fan-out Lambda: one POST /notify endpoint routes to Slack + SES per notification-routes.json.",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "package": "npm ci --omit=dev && zip -r deployment.zip . -x '*.zip' -x 'node_modules/.cache/*' -x 'README.md'",
    "deploy": "aws lambda update-function-code --function-name notification_hub --zip-file fileb://deployment.zip --profile chris-admin"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.911.0",
    "@aws-sdk/client-sesv2": "^3.911.0"
  }
}
